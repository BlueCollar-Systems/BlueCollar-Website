repos:
  - repo: local
    hooks:
      - id: private-artifact-guard
        name: Reject tracked PDF/CAD evidence
        entry: python tools/validate_private_artifacts.py
        language: system
        pass_filenames: false
        always_run: true
